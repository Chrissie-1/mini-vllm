[project]
name = "mini-vllm-worker"
version = "0.1.0"
description = "KV-cached, continuously batched LLM inference worker"
# numpy 2.5.x requires 3.12+; the pinned set cannot resolve below it.
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
filterwarnings = ["ignore::DeprecationWarning"]

[tool.setuptools.packages.find]
include = ["server*"]
